spring:
  application:
    name: rack-semanal-service

  cloud:
    config:
      enabled: true
      uri: http://localhost:8081

  datasource:
    url: jdbc:postgresql://localhost:5432/rack_semanal
    username: postgres
    password: holamundo

  jpa:
    hibernate:
      ddl-auto: update