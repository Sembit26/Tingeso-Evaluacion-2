spring:
  application:
    name: reserva-service

  cloud:
    config:
      enabled: true
      uri: http://localhost:8081

  datasource:
    url: jdbc:postgresql://localhost:5432/reserva_service
    username: postgres
    password: holamundo

  jpa:
    hibernate:
      ddl-auto: update

  mail:
    host: smtp.gmail.com
    port: 587
    username: sebamauricio26@gmail.com
    password: udgedhoohvrgffsl
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true