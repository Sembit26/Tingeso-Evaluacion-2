server:
  port: 8001

spring:
  application:
    name: descuento-por-cliente-frecuente

  cloud:
    config:
      enabled: true
      uri: http://localhost:8081

  datasource:
    url: jdbc:postgresql://localhost:5432/bdmicroservicios
    username: postgres
    password: holamundo

  jpa:
    hibernate:
      ddl-auto: update