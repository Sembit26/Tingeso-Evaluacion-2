server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/Sembit26/Tingeso-Evaluacion-2.git
          search-paths: config-data
  application:
    name: config-service