spring:
  application:
    name: tarifa-dias-especiales
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081

  datasource:
    url: jdbc:postgresql://localhost:5432/tarifa_especial
    username: postgres
    password: holamundo

  jpa:
    hibernate:
      ddl-auto: update